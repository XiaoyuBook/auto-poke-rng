# The interpreter is stdlib-only except for shared-frame image search and the
# selected PP-OCRv6 small runtime.
# NumPy 2.2 has no CPython 3.14 wheel. Keep the original pair for 3.12/3.13
# and use the first compatible wheel pair for newer Python releases.
numpy==2.2.6; python_version < "3.14"
opencv-python==4.12.0.88; python_version < "3.14"
numpy==2.3.3; python_version >= "3.14"
opencv-python==4.13.0.92; python_version >= "3.14"
rapidocr==3.9.2
onnxruntime==1.30.0
